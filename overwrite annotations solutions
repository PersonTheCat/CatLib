types to add:

** invertable set
safe registry
** value lookup
** dual map
** hash generator
** random chunk selector
commands ++

solutions for overwrite validator:

** disable auto imports
** delete all (<current package name>.)
** copy ALL imports from parent

improvements for overwrite validator:

** remove redundant imports (use a set)
** delete all paths used in imports
** remove original broken import fix (no longer needed)

** remove imports from original file before update content
